<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.NASAMediaExplorer" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:textColorHint">@color/text_color_black</item>
        <item name="android:colorControlNormal">@color/text_color_black</item>

        <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorPrimaryDark">@color/secondaryColor</item>
        <item name="colorSecondary">@color/notPrimaryColor</item>
        <item name="android:popupMenuStyle">@style/PopupMenu</item>

        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>

    <style name="Theme.NASAMediaExplorer" parent="Base.Theme.NASAMediaExplorer" />
</resources>